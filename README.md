# GreenTrace Demo Prototype
Game-style UI for the GreenTrace环保 platform (aligned with your wireframe).

## Setup
1. Unzip `demo_prototype.zip`
2. Install dependencies: `npm install`
3. Create `.env.local` (copy from `.env.local.example` and fill in secrets)
4. Run: `npm run dev`
5. Visit `http://localhost:3000`
